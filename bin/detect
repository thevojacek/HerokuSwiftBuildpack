#!/usr/bin/env bash
# bin/compile <build-dir>

BUILD_DIR=$1

echo $BUILD_DIR
echo $BUILD_DIR/Package.swift

cd $BUILD_DIR
ls -la

if [ -f BUILD_DIR/Package.swift ]; then
    echo "Swift application detected. Using Swift buildpack."
    exit 0
else
    exit 1
fi
